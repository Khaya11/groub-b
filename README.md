lab 1 object orientated programming & maven. The book tracker is a code that prints out the book name,author and pages.
it uses 2 java files a main and a book file. in the book file it uses Strings to initialise the title,  author, pagecount and availavility.
it has a system.out.println("x" + y) to print out all into the console in the book file.
in the main file it  uses the a new funtion firstBook."variable" to give the variable a value

you need a version later than 22.
to run main you just hit the run bottom on top right corner
public class is that so anyone can see the code in github. The object in our case is the book.
the method jut makes life easier cutting out alot of code by just using the Displaydetails function  by just taking out the + "x" and the end of every line of code.
the field represents the title, author, availability and page count of our code
just to keep the code more tidy
because if uses a if else statement
no
